# projet-ia-generative
Un RAG qui permet de recommander un métier à l'utilisateur en fonction des compétences dont il dispose.

# installation

fichier `requirements.txt` pour les paquets python :

`pip install -r requirements.txt`